# public/

Drop the two preschool photos into this folder before rendering:

- `scene1.jpg` — teacher showing the whole watermelon on the yellow tray
- `scene2.jpg` — teacher splitting the watermelon into halves

Remotion will pick them up via `staticFile('scene1.jpg')` and `staticFile('scene2.jpg')`.
